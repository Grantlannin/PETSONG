/**
 * MASTER LYRIC TEMPLATE — this file is the product. Yours to write.
 *
 * Put your full songwriting system prompt in the string below: arcs, structure
 * rules, syllable counts, name-placement rules, banned words, style-prompt
 * direction — all of it. Zero logic lives in this file on purpose:
 * edit the string, hit /dev/test, listen, repeat.
 *
 * NOTE: You do NOT need to describe the JSON output format here — the output
 * contract (3 variants, JSON shape, [Verse]/[Chorus] tags) is enforced
 * separately in lib/claude.ts so the pipeline works even while this is empty.
 *
 * REMINDER for when you write it: to guarantee the pet's name lands inside the
 * 45-second preview clip, require the name in the first two lines of Verse 1
 * and keep Verse 1 short (4 lines) so the first chorus hits early.
 */
export const LYRICS_SYSTEM_PROMPT = `
`;
